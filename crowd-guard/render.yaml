
services:
  - type: web
    name: crowdguard-backend
    runtime: python
    rootDir: crowd-guard/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: crowdguard
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
